
/**
 * Displays a chart of the added value
 * 
 * @author (your name) 
 * @version (a version number or a date)
 * instance variable: array
 * run: enter values manually (hardcode)
 * 
 * test
 * instance (entire class, can be public or private), parameter, local (only lives inside brackets). local to method = parameter
 * return vs void method
 * constructors
 * access specifier; public vs private
 * 
 */
import javax.swing.JFrame;
import javax.swing.JComponent; 
import java.awt.Graphics;
import java.awt.Graphics2D;
import java.awt.Rectangle;
import java.awt.Color;

public class BarChart
{
    public BarChart()
    {
        
    }
    
    public void add(double value)
    {
        
    }
    
    public void draw(Graphics2D g)
    {
        Graphics2D g2 = (Graphics2D)g;
    }
}
