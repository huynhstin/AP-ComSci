
/**
 * Checks to see if two rectangles overlap, and if true, returns area and fills with color
 * 
 * @author (Justin Huynh) 
 * @version (10/7/16)
 * g2.drawString(a + "", 0 ,0) // prints message at coords
 * int a = 1;
 * Rectangle r3 = r1.intersection(r2);
 */
public class IntersectionRectangle
{
    // instance variables - replace the example below with your own
    private int x;

    /**
     * Constructor for objects of class IntersectionRectangle
     */
    public IntersectionRectangle()
    {
        // initialise instance variables
        x = 0;
    }

    /**
     * An example of a method - replace this comment with your own
     * 
     * @param  y   a sample parameter for a method
     * @return     the sum of x and y 
     */
    public int sampleMethod(int y)
    {
        // put your code here
        return x + y;
    }
}
